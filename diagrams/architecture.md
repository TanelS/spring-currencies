# Application Architecture

> Generated by [Claude Code](https://claude.ai/code)

```mermaid
graph TD
    Client["REST Client / Swagger UI"]

    subgraph Exception["Exception Handling"]
        GEH["GlobalExceptionHandler\n@RestControllerAdvice"]
    end

    subgraph Controllers["Controllers"]
        RC["RateController\nGET /rates"]
        CC["CurrencyController\nGET /currencies"]
        TC["TestController\n/currency-testing/*\n(dev only)"]
    end

    subgraph Services["Services"]
        AS["ApiService\nCurrencyBeacon HTTP client"]
        CS["CurrencyService\nimport orchestration"]
        RS["RateService\nimport + query"]
    end

    subgraph Repositories["Repositories"]
        CR["CurrencyRepository\nJpaRepository + custom"]
        RR["RateRepository\nJpaRepository + @Query"]
    end

    subgraph Util["Utilities"]
        SC["StringCleaner\nJSON sanitizer"]
    end

    CB[("CurrencyBeacon API\nexternal")]
    DB[("PostgreSQL\nDocker Compose")]

    Client -->|HTTP| RC
    Client -->|HTTP| CC
    Client -->|HTTP| TC

    RC --> RS
    CC --> CS
    TC --> AS
    TC --> CS

    CS --> AS
    CS --> CR
    CS --> RS
    RS --> CR
    RS --> RR

    AS -->|RestClient| CB
    AS --> SC

    CR -->|JPA / StatelessSession| DB
    RR -->|JPA / native SQL| DB

    GEH -.->|intercepts exceptions from| Controllers
```

**Key design decisions:**
- `CurrencyService` and `RateService` are separate beans — `createRate()` is `@Transactional` and must be called through Spring's proxy, not via `this` inside `CurrencyService`.
- `getAllCurrencyCodes()` is a `default` method on `CurrencyRepository` — avoids circular injection between `CurrencyService` and `RateService`.
- `RateService.createRate()` uses Hibernate `StatelessSession` directly (bypassing JPA), reducing full import time from ~22 minutes to ~3m40s.

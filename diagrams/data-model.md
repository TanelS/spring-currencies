# Data Model

> Generated by [Claude Code](https://claude.ai/code)

```mermaid
erDiagram
    CURRENCY {
        bigint id PK
        text currency_name
        varchar(3) currency_code UK "ISO 4217 alpha code e.g. EUR"
        varchar(3) currency_num_code UK "ISO 4217 numeric code e.g. 978"
        int precision "decimal digits"
        int subunit "smallest unit multiplier"
        text symbol
        boolean symbol_first
        char decimal_mark
        char thousands_separator
        timestamp create_date
        timestamp modified_date
    }

    RATE {
        bigint id PK
        bigint currency_id FK "target currency"
        bigint base_currency_id FK "base currency"
        numeric rate
        timestamp rate_date "UTC, second precision"
        timestamp create_date
        timestamp modified_date
    }

    CURRENCY ||--o{ RATE : "as target (currency_id)"
    CURRENCY ||--o{ RATE : "as base (base_currency_id)"
```

**Notes:**
- `RATE` has a composite unique constraint on `(currency_id, base_currency_id, rate_date)` — prevents duplicate imports.
- Both foreign keys on `RATE` point at the same `CURRENCY` table (self-referencing via two distinct FK columns).
- `rate_date` is stored in UTC. Local-date queries use `AT TIME ZONE` in SQL to handle the Estonia 21:00–00:00 UTC window correctly.
- Base-currency-against-itself rates (e.g. EUR/EUR = 1.0) are not stored — returned in-memory by the query endpoint.
